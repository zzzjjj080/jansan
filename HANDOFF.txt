雀算（麻雀スコア記録iPhoneアプリ）の開発を、このチャットで続けたい。

前のスレッドが古くてスマホに出てこないので、新しく立て直したもの。
コードとドキュメントは全部そのまま残っているので、以下を読んで状況を把握してから始めてほしい。

■ まず読むもの

- ~/.claude/iOS-DEVLOG.md  … iPhoneアプリ開発の引き継ぎ書（罠17件・リリース手続き・設計の勘所）
- ~/Claude/Jansan/          … リポジトリ本体。コミットメッセージに「なぜそうしたか」を日本語で書いてある

■ アプリの概要

- 麻雀のスコアを記録する。4人打ちのほか5〜6人の卓にも対応
- 3人分の点数を入れると4人目は自動計算（合計0になるので逆算できる）
- com.zzzjjj080.Jansan / App ID 6802013584 / 無料 / 日本のみ配信
- https://github.com/zzzjjj080/jansan
- 構成は JansanCore（UI非依存のロジック＋テスト）+ Jansan（SwiftUI）

■ いまの状態（2026-09-01時点）

配信済み: 1.0 (2)
アップロード済み: 1.1 (3) を2026-08-19に App Store Connect へ上げた

★ 確認してほしいこと: 1.1 が実際に提出・配信されたかが分からない。
  App Store Connect を見るか、iPhoneのApp Storeで雀算のページを開いて
  「アップデート」が出るかどうかで判別できる。
  未提出なら提出まで進めたい。文面は store/whatsnew-1.1.txt に用意済み。

  これが気になるのは、1.1に「名前を変えると点数が丸ごと消える」不具合の修正が
  入っているため。未提出なら、その不具合が今も配信され続けていることになる。

■ 作業中のものがある（重要）

別のセッションが投げ銭とフィードバック機能を作っていて、コミットされていない変更が残っている。

  コミット済み: c19deb5 投げ銭「開発者にコーヒーを奢る」を入れる
               （CoffeeTipSection.swift / TipProduct.swift / TipJar.swift）
               商品ID: com.zzzjjj080.Jansan.coffee

  未コミット:   Jansan/Jansan/Feedback.swift（新規・不具合報告をメールで送る受け口）
               Jansan/Jansan/SettingsView.swift（上の2つを設定画面に差し込む変更）

**着手前に必ず git status を見て、この作業中の変更を壊さないこと。**
別のチャットが今も触っている可能性がある。

■ 守ってほしいこと

- デバッグしたら、その回のうちに実機（iPhone Air）へ最新ビルドを入れる。
  開発用の実機はAir 1台のみ。iPhone 15は手放した
- タップやチェックには触覚フィードバックを付ける（全アプリ共通）
- 「ビルドが通った」で終わらせず、シミュレータで実際にタップして確かめる
- 推測で報告しない。バイナリを検索する、実際に起動する、で裏を取る
- 審査中にビルドを差し替えると審査がやり直しになる。
  コミットはしてよいが、アップロードは審査結果が出てから

■ 雀算で過去に踏んだ罠（同じ失敗を繰り返さないため）

- 参加者を名前で突き合わせて列を組み直す実装なので、改名に setPlayers を使うと
  別人扱いになって点数が消える。改名は renamePlayer を使う
- ForEach に添字を渡すと、メンバー削除で落ちる。要素そのものを回してIDで操作する
- マスを消す操作で再計算を走らせると、別のマスが巻き添えで消える。
  消すのは消すだけにして、再計算は次に値が確定したときへ回す
- .storekit をプロジェクト配下に置くとリリースビルドに同梱される
- アップロードは App Store Connect のAPIキー経由で通す。
  Xcodeのアカウント経由は「Failed to Use Accounts」で止まることがある
  キーID CH8R5RJGXQ / Issuer cfeb84ca-47e6-45b2-8c5f-192212240b6c

■ 未実装・保留

- カンパ（App内課金）: 上記のとおり実装は入ったが、動作確認は未。
  銀行口座と税務情報の登録状況しだい

まずは git status と直近のコミットを見て、いまどうなっているか教えてほしい。
